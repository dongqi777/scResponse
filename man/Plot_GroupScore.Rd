% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plot_GroupScore.R
\name{Plot_GroupScore}
\alias{Plot_GroupScore}
\title{Plot group-wise distribution of enrichment scores}
\usage{
Plot_GroupScore(
  meta,
  group = "CellType",
  value = "RScore",
  fill = "Response",
  ViolinWidth = 0.9,
  ViolinAlpha = 0.9,
  BoxplotWidth = 0.2,
  BoxAlpha = 1,
  BoxColor = "white",
  bgcolor = "#F1F0E4"
)
}
\arguments{
\item{meta}{A \code{data.frame} containing per-unit metadata.
Rows correspond to analysis units.}

\item{group}{Character scalar. Column name in \code{meta} defining
the grouping variable on the x-axis.
Default is \code{"CellType"}.}

\item{value}{Character scalar. Column name in \code{meta} containing
enrichment scores to be visualized.
Default is \code{"RScore"}.}

\item{fill}{Character scalar. Column name in \code{meta} used to
determine fill colors of the violins and boxplots.
Default is \code{"Response"}.}

\item{ViolinWidth}{Numeric. Width of the violin plots.
Default is \code{0.9}.}

\item{ViolinAlpha}{Numeric. Transparency level of the violin plots.
Default is \code{0.9}.}

\item{BoxplotWidth}{Numeric. Width of the boxplots.
Default is \code{0.2}.}

\item{BoxAlpha}{Numeric. Transparency level of the boxplots.
Default is \code{1}.}

\item{BoxColor}{Character. Color of the boxplot outlines.
Default is \code{"white"}.}

\item{bgcolor}{Character. Background color of the plot panel.
Default is \code{"#F1F0E4"}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Visualize the distribution of enrichment (response) scores across
predefined groups of analysis units using violin and box plots.
}
\details{
This function operates directly on a metadata \code{data.frame},
making it independent of any specific transcriptomic modality
or analysis framework (e.g. single-cell or spatial data).
Each row in \code{meta} is assumed to represent one analysis unit,
such as a cell or a spatial spot.

Groups on the x-axis are ordered by the median value of the
enrichment score within each group, in decreasing order.
}
